'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '@/lib/supabase-browser';import AdminGuard from '@/components/AdminGuard';import AdminNav from '@/components/AdminNav';
function Participants(){const sb=supabaseBrowser();const [items,setItems]=useState<any[]>([]);async function load(){const {data:q}=await sb.from('quizzes').select('id').eq('is_public',true).order('created_at',{ascending:false}).limit(1).maybeSingle();if(q){const {data}=await sb.from('participants').select('participant_code,full_name,club_name,city,status,created_at').eq('quiz_id',q.id).order('created_at',{ascending:true});setItems(data||[])}}useEffect(()=>{load()},[]);return <main className="section"><AdminNav/><div className="eyebrow">Registration list</div><h1>Participants</h1><div className="card"><div className="tablewrap"><table className="table"><thead><tr><th>Code</th><th>Name</th><th>Club</th><th>City</th><th>Status</th><th>Registered</th></tr></thead><tbody>{items.map(x=><tr key={x.participant_code}><td>{x.participant_code}</td><td>{x.full_name}</td><td>{x.club_name}</td><td>{x.city||'—'}</td><td>{x.status}</td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></div></main>}
export default function Page(){return <AdminGuard><Participants/></AdminGuard>}
