'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Join(){const [code,setCode]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const res=await fetch('/api/join',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({participantCode:code.trim().toUpperCase()})});const j=await res.json();if(!res.ok)throw new Error(j.error||'Unable to join');sessionStorage.setItem('participant_session',j.sessionToken);sessionStorage.setItem('participant_code',j.participantCode);sessionStorage.setItem('participant_name',j.fullName);r.push(j.status==='LIVE'?'/quiz':'/waiting')}catch(e){setError(e instanceof Error?e.message:'Unable to join');}finally{setLoading(false)}}return <main className="section"><div className="card" style={{maxWidth:560,margin:'auto'}}><div className="eyebrow">Event day</div><h1>Join the live quiz</h1><p className="muted">Enter the participant code you received after registration.</p>{error&&<div className="error">{error}</div>}<form className="form" onSubmit={submit}><div className="field"><label>Participant Code</label><input required autoCapitalize="characters" placeholder="MM26-XXXXXX" value={code} onChange={e=>setCode(e.target.value)}/></div><button className="btn btn-primary" disabled={loading}>{loading?'Joining…':'Join Quiz'}</button></form></div></main>}
